const express = require('express');
const router = express.Router();

// controller에서 정의한 함수
// const {
//   registerLecture
// } = require('../controllers/lectureController');

// '/api/lectures' 경로에 대한 라우트 설정
// router.post('/register', registerLecture); // 강의 등록

module.exports = router;