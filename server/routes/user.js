// 예시 코드
// const express = require('express');
// const router = express.Router();
// const userController = require('../controllers/userController'); // 작업반장을 불러옵니다.

// // '/api/users/register' 주소로 POST 요청이 오면,
// // userController.registerUser 함수를 실행하도록 연결합니다.
// router.post('/register', userController.registerUser);
// 즉 콜백 함수를 여기서 정의하지 않고 controller에서 정의해놓은 함수로 사용

// module.exports = router;